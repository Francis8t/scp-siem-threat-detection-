# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# --- Secrets / credentials (never commit) ---
.env
*.pem
*.key
.aws/
credentials

# --- Data: keep fixtures/OpenSSH_2k.log, exclude the full 70MB log + archives ---
SSH.tar.gz
SSH.log
*.tar.gz
OpenSSH.log
fixtures/OpenSSH.log

# --- macOS / editor ---
.DS_Store
.idea/
*.swp

# --- Spark / EMR local junk ---
metastore_db/
derby.log
spark-warehouse/

# --- Benchmark run artifacts ---
# Console logs and the partial CSVs from interrupted runs. The authoritative
# results are experiment1_raw.csv / experiment1_summary.csv, rebuilt from AWS
# by benchmarks/recover_experiment1.py, and experiment2_raw.csv.
benchmarks/*.log
benchmarks/experiment1_raw.n1-only.csv
benchmarks/experiment1_raw.runner-partial.csv
